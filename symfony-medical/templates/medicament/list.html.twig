{% block list_block %}
    <table>
        
        {% for a in list %}
        <tr>
            <td>{{ a.denomination }}</td>
            <td>{{ a.conditionnement }}</td>
        </tr>
        {% else %}<tr>
            <td>Aucun {{ label }} n'a été trouvé.</td>
        </tr>
        {% endfor %}
    </table>
{% endblock %}